[pytest]
timeout = 300
asyncio_mode = auto
testpaths = tests
markers =
    integration: marks tests as integration (requires LLM server, excluded from pre-commit)
    slow: marks tests as slow (excluded from quick validation sweeps)
addopts = -m "not integration"
