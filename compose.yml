---
services:
  repository-manager-agent:
    # image: docker.io/knucklessg1/repository-manager:latest
    build:
      context: . # Debug
      dockerfile: debug.Dockerfile
    container_name: repository-manager-agent
    hostname: repository-manager-agent
    command: [ "repository-manager-agent" ]
    volumes:
      - ./mcp/workspace:/workspace:rw
    working_dir: /workspace
    extra_hosts:
      - "host.docker.internal:host-gateway"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    restart: always
    env_file:
      - .env
    environment:
      - "PYTHONUNBUFFERED=1"
      - "HOST=0.0.0.0"
      - "PORT=9147"
      - "TRANSPORT=stdio"
      - "REPOSITORY_MANAGER_WORKSPACE=/workspace"
      - "REPOSITORY_MANAGER_THREADS=12"
      - "REPOSITORY_MANAGER_DEFAULT_BRANCH=True"
      - "PROVIDER=openai"
      - "LLM_BASE_URL=${LLM_BASE_URL:-http://host.docker.internal:1234/v1}"
      - "LLM_API_KEY=${LLM_API_KEY:-llama}"
      - "MODEL_ID=${MODEL_ID:-qwen/qwen3-coder-next}"
      - "DEBUG=False"
      - "ENABLE_WEB_UI=True"
      - "ENABLE_OTEL=True"
      - "REPOSITORY_MANAGER_WORKSPACE=/workspace"
    ports:
      - "9147:9147"
    healthcheck:
      test: [ "CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:9147/health')" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
